---
apiVersion: v1
kind: Secret
metadata:
  name: lore
stringData:
  # Change this before deploying outside a disposable test cluster.
  postgres-password: change-me
  # Keep the password in this URL in sync with postgres-password above.
  database-url: postgres://lore:change-me@postgres:5432/lore?sslmode=disable
