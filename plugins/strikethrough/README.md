# Strikethrough

Strikethrough adds GitHub-style `~~text~~` syntax to Lore Markdown.

## Usage

```markdown
Deploy ~~Friday~~ Monday.
```

When the plugin is disabled, the source remains ordinary Markdown text and the tildes are preserved.

## Permissions

This plugin requests no Lore capabilities. It selects Lore's public `strikethrough` Markdown grammar.
