# Definition Lists

Definition Lists adds term/definition list syntax to Lore Markdown.

## Usage

```markdown
Runbook
: A repeatable operational procedure.

Owner
: Platform team
```

Definition lists are disabled by default. Enable this plugin when the wiki should recognize this syntax.

## Permissions

This plugin requests no Lore capabilities. It selects Lore's public `definition-list` Markdown grammar.
